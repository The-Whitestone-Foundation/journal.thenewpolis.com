<script type="application/ld+json">
{% set siteUrl = metadata.url | htmlBaseUrl %}
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "{{ '/' | htmlBaseUrl(siteUrl) }}"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "{{ title }}",
      "item": "{{ page.url | htmlBaseUrl(siteUrl) }}"
    }
  ]
}
</script>
